// import { getLatestTransactions } from '../../src/batch';
import { PAIR_ADDRESS } from '../../__mocks__/mock_constants';

describe('getLatestTransactions', () => {
  it.skip('should return latest transactions data', async () => {
    // await getLatestTransactions(0);
    expect(PAIR_ADDRESS).toBe(PAIR_ADDRESS);
  });
});
