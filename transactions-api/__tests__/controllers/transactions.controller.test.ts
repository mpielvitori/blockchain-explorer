// import { getTransactions } from '../../src/controllers';
import { TRANSACTION, PAIR_ADDRESS } from '../../__mocks__/mock_constants';

describe.skip('getTransactions', () => {
  it('should return transactions data', async () => {
    // const req = {
    //   query: { contract: PAIR_ADDRESS },
    // };
    // const res = {
    //   send: jest.fn(),
    // };
    // await getTransactions(req, res);
    // expect(res.send).toHaveBeenCalledWith([TRANSACTION]);
  });
});
